# ----------------------------------------------------------------------------
#
#  Welcome to Baml! To use this generated code, please run the following:
#
#  $ pip install baml
#
# ----------------------------------------------------------------------------

# This file was generated by BAML: please do not edit it. Instead, edit the
# BAML files and re-generate this code using: baml-cli generate
# baml-cli is available with the baml package.

_file_map = {

    "categorize.baml": "class TagOption {\n  slug string\n  description string? @description(\"When to apply this tag\")\n}\n\nclass CategorizeAndTagResult {\n  categories ArticleCategory[] @description(\"1-2 categories\")\n  kind ArticleKind\n  tags string[] @description(\"1-3 slugs copied verbatim from the vocabulary; most relevant first\")\n}\n\nclass ClassifyKindResult {\n  kind ArticleKind\n}\n\nfunction CategorizeAndTag(title: string, content: string, vocabulary: TagOption[]) -> CategorizeAndTagResult {\n  client Nvidia\n  prompt #\"\n    {{ _.role(\"system\") }}\n    You are an editor and taxonomist for a developer news digest. Your readers are tech-savvy but busy. You assign topic tags from a fixed controlled vocabulary and never invent tags.\n\n    {{ _.role(\"user\") }}\n    Classify and tag this article.\n\n    Title: {{ title }}\n\n    Content:\n    {{ content }}\n\n    1. Assign 1-2 categories.\n    2. Assign a kind.\n    3. Assign 1-3 tags from the vocabulary below, most relevant first. Pick the smallest set that captures what the article is actually about - do not pad to three. Use slugs from the vocabulary ONLY, copied exactly as written.\n\n    Category definitions:\n    - Models: New model releases, benchmarks, evals, model cards, model comparisons\n    - Dev: Repos, tools, product launches, developer blogs, tutorials, how-tos, fine-tuning guides, deployment recipes, infrastructure updates\n    - Research: Papers, breakthroughs, novel techniques with practical implications\n\n    Kind definitions:\n    - Repo: GitHub or GitLab repository\n    - Paper: Academic paper, preprint, or long-form technical report\n    - Model: Model card or model release page (e.g. HuggingFace)\n    - Blog: Developer blog post, tutorial, how-to, or opinion piece\n    - Product: Product launch page or product website\n    - Announcement: Official release post from a major AI lab (OpenAI, Anthropic, Google, Meta, Mistral, etc.)\n\n    Tag vocabulary — use these slugs ONLY, copied exactly as written:\n    {% for t in vocabulary %}\n    - {{ t.slug }}{% if t.description %}: {{ t.description }}{% endif %}\n    {% endfor %}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\nfunction ClassifyKind(title: string, url: string) -> ClassifyKindResult {\n  client Nvidia\n  prompt #\"\n    {{ _.role(\"system\") }}\n    You are a technical librarian who classifies AI developer content by format.\n\n    {{ _.role(\"user\") }}\n    Classify this article's kind.\n\n    Title: {{ title }}\n    URL: {{ url }}\n\n    Kind definitions:\n    - Repo: GitHub or GitLab repository\n    - Paper: Academic paper, preprint, or long-form technical report\n    - Model: Model card or model release page (e.g. HuggingFace)\n    - Blog: Developer blog post, tutorial, how-to, or opinion piece\n    - Product: Product launch page or product website\n    - Announcement: Official release post from a major AI lab (OpenAI, Anthropic, Google, Meta, Mistral, etc.)\n\n    {{ ctx.output_format }}\n  \"#\n}\n\ntest categorize_and_tag_basic {\n  functions [CategorizeAndTag]\n  args {\n    title \"Anthropic releases Claude 4\"\n    content \"Anthropic today launched Claude 4, a new flagship model. It features improved reasoning, a 1M-token context window, and native tool use. The model is available via API and Claude.ai starting today.\"\n    vocabulary [\n      {\n        slug \"models\"\n        description \"new model releases, benchmarks, evals\"\n      }\n      {\n        slug \"agents\"\n        description \"autonomous agents: reasoning loops, planning\"\n      }\n    ]\n  }\n}\n",
    "clients.baml": "// The provider caps how many requests may be in flight at once, far below its\n// per-minute quota: measured 2026-09-03, 5 parallel calls all answered while 10\n// returned 429 in ~90ms with x-ratelimit-remaining still at 1980/2000, and the\n// cap refills within ~10s. So a 429 here means \"too many at once\", never \"out\n// of budget\", and waiting is the correct response. Without this policy those\n// bursts fell through to the fallback client instead.\nretry_policy Burst {\n  max_retries 4\n  strategy {\n    type exponential_backoff\n    delay_ms 500\n    multiplier 2\n    max_delay_ms 8000\n  }\n}\n\n// First choice since 2026-09-13: ScoreArticles calls it directly and the Nvidia\n// wrapper below tries it first. Second in line from 2026-09-03, replacing\n// NVIDIA's gpt-oss-120b, which reached end of life that morning and answered\n// every call with 410 Gone - dead weight for hours before anyone noticed,\n// because a fallback only speaks up when the primary fails.\n//\n// Measured 2026-09-03 on a SelectProductLink-shaped prompt: 1.5-6.8s across\n// sequential calls, all answering with clean JSON and the right pick — no sign\n// of the 52-285s latency that made gpt-oss-120b unusable, though that was a\n// property of the endpoint under load, so it is worth re-measuring if runs\n// start crawling. Past 8 parallel calls NIM starts returning 503; the retry\n// policy covers those.\n//\n// Qwen3.5-397B was the other candidate and judged just as well,\n// but it is a reasoning model: it spends its budget in a `reasoning` field and\n// emits an EMPTY content string if it runs out before it stops thinking (427\n// completion tokens for a two-element array). That needs a large explicit\n// max_tokens to be safe.\nclient<llm> Nemotron {\n  provider openai-generic\n  retry_policy Burst\n  options {\n    base_url env.NVIDIA_NIM_BASE_URL\n    api_key env.NVIDIA_NIM_API_KEY\n    model \"nvidia/nemotron-3-super-120b-a12b\"\n    // The timeout lives in `http`, which BAML applies itself. A bare `timeout`\n    // option is sent as a body param, and NIM answers that with 400\n    // \"Unsupported parameter(s)\": every call here failed that way from\n    // 2026-09-04 until this block replaced it.\n    http {\n      request_timeout_ms 120000\n    }\n  }\n}\n\n// Second in the Nvidia fallback, and --model gpt-oss-20b in the rescore scripts\n// (scripts/rescoring.py). The smaller sibling of the gpt-oss-120b that led\n// scoring until its endpoint reached end of life. Only measured on\n// ScoreArticles so far (2026-09-13): clean JSON, but it read the rubric's\n// 70 floor for lab releases as a cap.\nclient<llm> GptOss20b {\n  provider openai-generic\n  retry_policy Burst\n  options {\n    base_url env.NVIDIA_NIM_BASE_URL\n    api_key env.NVIDIA_NIM_API_KEY\n    model \"openai/gpt-oss-20b\"\n    http {\n      request_timeout_ms 120000\n    }\n  }\n}\n\n// Last in the Nvidia fallback since 2026-09-13; primary from 2026-08-06 until\n// then. Fast (2-3s) and reliable, but a lower-quality judge - see the variance\n// warning on the Nvidia wrapper below before moving it back up. It stays as\n// the last resort because it is the only client not on NIM: when NIM refuses a\n// burst, the two clients above it fail together.\nclient<llm> Ministral {\n  provider openai-generic\n  retry_policy Burst\n  options {\n    base_url env.MISTRAL_AI_BASE_URL\n    api_key env.MISTRAL_AI_KEY\n    model \"mistralai/Ministral-3-14B-Instruct-2512\"\n    // A real client-side timeout. The old `timeout 120` went out as a body\n    // param, which this endpoint silently ignored: it never timed anything out.\n    http {\n      request_timeout_ms 120000\n    }\n  }\n}\n\n// The name every function already references. A fallback wrapper: Nemotron\n// first, then gpt-oss-20b, then Ministral. Renaming would mean editing all 12\n// `client Nvidia` sites for no gain, so the name stays - and with two NIM\n// models in front it is mostly true again.\n//\n// A Gemini Flash client sat third here until 2026-09-03, when the run that\n// needed it got 400 FAILED_PRECONDITION \"User location is not supported for\n// the API use\" - the Google AI API does not serve the prod VPS's region, so\n// the last resort had never been able to answer. Removed rather than left in\n// place: a client that always fails is not a fallback, it just hides how thin\n// the chain really is. A replacement has to be reachable from the box.\n//\n// ScoreArticles no longer runs through this wrapper (2026-09-13): it calls\n// Nemotron directly. Ministral-3-14B judged with ±15 run-to-run variance on\n// identical input, intermittently returned fewer results than it was given\n// (apply_scores reads a missing score as 0, a silent reject), and over the ten\n// runs before the switch put 15 articles at 90+ and passed items the rubric\n// rules out. A fallback to it would mix two judges' scales in one feed, so\n// scoring has none.\nclient<llm> Nvidia {\n  provider fallback\n  options {\n    strategy [Nemotron, GptOss20b, Ministral]\n  }\n}\n",
    "discover.baml": "// Batched relevance classifier for the Substack discovery crawler.\n// One LLM call judges many candidate profiles at once (see ScoreArticles for\n// the array-in/array-out batching pattern). Classification runs on cheap\n// metadata (name + description) BEFORE any per-profile post fetch, and gates\n// both DB insertion and whether the crawler recurses into that profile.\n\nclass ProfileInput {\n  handle string @description(\"Substack owner handle, echoed back in the verdict\")\n  name string @description(\"Publication name\")\n  description string? @description(\"Publication hero text and/or author bio, if known\")\n}\n\nclass ProfileVerdict {\n  handle string @description(\"Must exactly match the input handle\")\n  isAiRelated bool\n  confidence int @description(\"0-100 confidence that AI/ML/LLMs is the PRIMARY focus\")\n}\n\nfunction ClassifyProfiles(profiles: ProfileInput[]) -> ProfileVerdict[] {\n  client Nvidia\n  prompt #\"\n    {{ _.role(\"system\") }}\n    You curate sources for a developer-focused AI news platform. For each Substack\n    publication, decide whether artificial intelligence is its PRIMARY subject:\n    AI/ML/LLMs, foundation models, AI engineering, AI products, or AI research.\n\n    Mark isAiRelated = true ONLY when AI is clearly the main topic. Mark false for\n    general tech, productivity, marketing, business, crypto, politics, lifestyle, or\n    publications that merely mention AI in passing. When the signal is thin (sparse\n    description, generic name), lean false and assign low confidence - the crawler\n    prunes everything you reject, so false positives pollute the whole subtree.\n\n    {{ _.role(\"user\") }}\n    Classify every profile. Echo each handle back EXACTLY as given.\n\n    {% for p in profiles %}\n    {{ loop.index }}. handle={{ p.handle }} | {{ p.name }}{% if p.description %} — {{ p.description }}{% endif %}\n    {% endfor %}\n\n    {{ ctx.output_format }}\n  \"#\n}\n",
    "extract_items.baml": "// Newsletter extraction.\n//\n// A newsletter reports two kinds of thing worth ingesting, and they need\n// different URL resolution:\n//\n// - Product: a named launch (model, tool, SDK). Its canonical URL is the\n//   vendor's own page, which the email rarely links to directly, so the URL is\n//   rediscovered by web search (SelectProductLink) rather than trusted from the\n//   email. This replaced the per-newsletter tracking-URL decoders.\n// - Article: a blog post, essay, paper, or write-up. Here the link IS the\n//   subject — there is nothing to search for, and a search would land on some\n//   other page about the same topic. Its href is kept and followed instead\n//   (see ``_resolve_article`` in pipeline/sources/email.py), which turns a\n//   tracking redirect into the real destination.\n//\n// Hence NewsletterItem.url: the model must copy an article's href verbatim\n// even though it is usually a redirect, because following it is exactly how\n// the real URL is recovered.\n\nenum NewsletterItemKind {\n  Product  // A named product, tool, model, SDK, dataset, or launch\n  Article  // A blog post, essay, paper, guide, or write-up\n}\n\nclass NewsletterItem {\n  kind NewsletterItemKind\n  name string @description(\"Product name, or the article's title\")\n  description string @description(\"Concise phrase: what it is and does, usable verbatim as a web search query\")\n  url string? @description(\"For an Article, the href of its link, copied verbatim including any tracking redirect. Omit for a Product.\")\n}\n\nfunction ExtractItems(newsletterText: string) -> NewsletterItem[] {\n  client Nvidia\n  prompt #\"\n    {{ _.role(\"system\") }}\n    You extract the AI/tech items a newsletter is reporting on: named products, and the articles it links to. Work only from the text; never invent items that are not mentioned.\n\n    {{ _.role(\"user\") }}\n    Below is the text of a newsletter email. Links appear as [anchor text](url).\n\n    Identify every distinct item the newsletter reports as news, and classify each:\n\n    - Product — a named product, tool, model, SDK, dataset, or launch a reader could go use. Judge it by the words, not the link: its URL is looked up separately, so do NOT return a url for a Product.\n    - Article — a blog post, essay, paper, guide, benchmark write-up, or analysis the newsletter is pointing at. For these the link is the subject, so ALWAYS return its url, copied verbatim from the text — including tracking redirects, which are followed later to recover the real destination. An item with no link is not an Article.\n\n    For each item return:\n    - kind: Product or Article\n    - name: the exact product name, or the article's title\n    - description: a concise phrase describing what it is and does, suitable as a web search query\n    - url: the article's href (Article only)\n\n    A single story often yields one item, not two: if a link is the vendor's own announcement of a named launch, return it as a Product. Return an Article when the piece itself is the point — someone's write-up, teardown, guide, or opinion.\n\n    INCLUDE only real, named launches and substantive articles a reader could go use or read.\n\n    IGNORE:\n    - the author's intro letter and personal commentary\n    - sponsor blurbs, advertisements, \"brought to you by\", upgrade / subscribe / manage-preferences prompts\n    - social-media embeds (an @handle with like / repost / reply counts) and the newsletter's own social links\n    - section headers, navigation, and unsubscribe links\n    - job postings\n\n    Newsletter text:\n    {{ newsletterText }}\n\n    If the text names no qualifying items, return an empty list.\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// Notability gate: trims the extracted list to the items actually worth\n// resolving, so we don't spend a web search on every incremental SDK bump.\n// Returns the 1-based indices of the items to KEEP.\nfunction SelectNotableItems(items: NewsletterItem[]) -> int[] {\n  client Nvidia\n  prompt #\"\n    {{ _.role(\"user\") }}\n    Below is a numbered list of items a newsletter mentioned. Return the numbers of the ones that are NOTABLE enough to feature in an AI-news aggregator.\n\n    KEEP an item if it is distinct, real, and individually newsworthy: new models, significant new products or platforms, notable agents/tools, major releases, and articles with real substance — technical write-ups, benchmarks, teardowns, engineering posts, or arguments a developer would want to read.\n\n    DROP:\n    - minor or incremental items: small SDK/utility bumps, niche helper libraries, design skills, config tweaks\n    - vague or marketing-only entries with no concrete product behind them\n    - listicles, SEO filler, press-release rewrites, and thin news blurbs that only restate an announcement\n    - duplicates or sub-features of another item in the list\n\n    Items:\n    {% for item in items %}\n    {{ loop.index }}. [{{ item.kind }}] {{ item.name }} — {{ item.description }}\n    {% endfor %}\n\n    Return only the numbers to keep, as a JSON array of integers. If none qualify, return [].\n\n    {{ ctx.output_format }}\n  \"#\n}\n\nclass SearchCandidate {\n  title string\n  url string\n  snippet string\n}\n\nclass ProductLinkChoice {\n  index int @description(\"1-based index of the best first-party result, or 0 if none qualifies\")\n  confidence float @description(\"0.0 - 1.0\")\n}\n\n// Picks the best canonical URL for a product among search candidates, biased\n// hard toward the official / first-party source and away from social posts and\n// SEO/aggregator pages. Returns index 0 when nothing is clearly the product.\n// Products only — an article already carries its own link.\nfunction SelectProductLink(\n  productName: string,\n  productDescription: string,\n  candidates: SearchCandidate[]\n) -> ProductLinkChoice {\n  client Nvidia\n  prompt #\"\n    {{ _.role(\"user\") }}\n    A newsletter featured this item:\n      name: {{ productName }}\n      description: {{ productDescription }}\n\n    Web search returned these candidate results:\n    {% for c in candidates %}\n    {{ loop.index }}. {{ c.title }}\n       url: {{ c.url }}\n       snippet: {{ c.snippet }}\n    {% endfor %}\n\n    Pick the ONE candidate that is the canonical, first-party source for this exact product — its official site, product page, model card, repo, or the vendor's own announcement.\n\n    PREFER the official / first-party domain that matches the product or its maker.\n    REJECT (do not pick) when it is the best available:\n    - social-media posts (x.com, twitter.com, linkedin.com, reddit.com, threads, mastodon)\n    - videos (youtube.com)\n    - content-farm / SEO / generic aggregator or listicle pages that merely mention the product\n    - a DIFFERENT product that happens to share the name\n\n    Return the 1-based index of the best first-party result. If none of the candidates is clearly a first-party source for THIS product, return index 0.\n\n    {{ ctx.output_format }}\n  \"#\n}\n\ntest extract_items_basic {\n  functions [ExtractItems]\n  args {\n    newsletterText \"Headlines: [OpenAI ships GPT-5](https://sub.stack/redirect/2/abc), a new flagship model. Deep dive: [Why agent harnesses fail](https://stencil.so/blog/harness-playbook?utm_source=tldrai) walks through production agent design. Brought to you by Acme. [Unsubscribe](https://newsletter.com/unsub).\"\n  }\n}\n",
    "extract_links.baml": "class ExtractedLink {\n  title string\n  url string\n  snippet string @description(\"One-sentence description of the article\")\n}\n\nfunction ExtractLinks(emailHtml: string) -> ExtractedLink[] {\n  client Nvidia\n  prompt #\"\n    {{ _.role(\"system\") }}\n    You are a precise link extraction engine. You parse newsletter HTML and identify external article links while filtering out tracking, admin, and promotional URLs.\n\n    {{ _.role(\"user\") }}\n    You are given the HTML body of a newsletter email. Extract all article links that point to external news stories, blog posts, research papers, or product announcements.\n\n    For each article, return its title, URL, and a one-sentence snippet describing it.\n\n    IGNORE these types of links:\n    - Tracking/redirect URLs (e.g. links through email marketing platforms)\n    - Unsubscribe, manage preferences, or email settings links\n    - Social media profile links (twitter, linkedin, etc.)\n    - Links back to the newsletter's own homepage, archive index, or subscription page (but DO include article/post links hosted on the newsletter's domain - curated newsletters often host their own content)\n    - Sponsor/advertisement links\n    - Job posting links\n\n    RESOLVE redirect URLs: if a link is clearly a tracking redirect (contains /click, /track, utm_ params, etc.), try to extract the final destination URL from the href. Strip all UTM parameters from URLs.\n\n    Newsletter HTML:\n    {{ emailHtml }}\n\n    If no relevant article links are found, return an empty list.\n\n    {{ ctx.output_format }}\n  \"#\n}\n\ntest extract_links_basic {\n  functions [ExtractLinks]\n  args {\n    emailHtml \"Welcome to today's issue! [Read OpenAI's new paper](https://openai.com/papers/gpt-5). [Unsubscribe](https://newsletter.com/unsubscribe).\"\n  }\n}\n",
    "fix_titles.baml": "class TitleFix {\n  url string @description(\"The exact URL from the matching input article - copy verbatim\")\n  title string @description(\"Plain English title text on a single line. No markdown, no quotes, no leading source name or bracket tag. Max 12 words. Return the input title verbatim if it is already clear, specific, and informative.\")\n}\n\nfunction ImproveTitles(articles: ArticleInput[]) -> TitleFix[] {\n  client Nvidia\n  prompt #\"\n    {{ _.role(\"system\") }}\n    You are a technical editor. You write clear, informative article titles for an AI developer news platform.\n\n    Output rules (strict):\n    - For each input article, return one TitleFix whose `url` exactly matches that article's URL.\n    - The `title` field contains plain title text ONLY. Do NOT wrap it in quotes. Do NOT prefix it with \"[Source]\" or any source name. Do NOT include the URL.\n    - Write in English. Never emit any other language or script.\n    - No markdown. Do NOT wrap the title in ** or * or _ or backticks.\n    - One single line. No newlines, no braces, no brackets, no JSON.\n    - Every title must be a complete phrase. Never truncate mid-word or mid-sentence.\n    - Use only the snippet of the SAME article to inform the rewrite. Never blend information across articles.\n\n    {{ _.role(\"user\") }}\n    Decide for each article whether the existing title is already good. A good title is specific, informative, and technical/neutral in register - it tells a developer what the article is actually about.\n\n    - If the existing title is already good, return it verbatim.\n    - Otherwise rewrite it using only that article's snippet: specific, informative, between 3 and 12 words, technical/neutral register, no hype or filler.\n    - If the snippet is empty or too thin to write an accurate title from, return the existing title verbatim rather than inventing one.\n\n    {% for a in articles %}\n    --- Article {{ loop.index }} ---\n    url: {{ a.url }}\n    source: {{ a.source }}\n    current_title: {{ a.title }}\n    {% if a.snippet %}snippet: {{ a.snippet }}{% endif %}\n    {% endfor %}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\ntest improve_titles_rewrite_short {\n  functions [ImproveTitles]\n  args {\n    articles [\n      {\n        url \"https://example.com/claude\"\n        title \"Claude 4\"\n        source \"HN\"\n        snippet \"Anthropic today released Claude 4, a new flagship model with improved reasoning and a 1M-token context window.\"\n      }\n    ]\n  }\n}\n\ntest improve_titles_keep_unchanged {\n  functions [ImproveTitles]\n  args {\n    articles [\n      {\n        url \"https://example.com/post\"\n        title \"Anthropic releases Claude 4 with 1M-token context window\"\n        source \"HN\"\n        snippet \"Anthropic today released Claude 4, a new flagship model with improved reasoning.\"\n      }\n    ]\n  }\n}\n\n// ─── Regression tests ───────────────────────────────────────────────────────\n// Real inputs from the production `article` table that produced corrupted\n// titles. Note the snippets are themselves already dirty (markdown bold,\n// stray CJK) — Summarize emitted them, and ImproveTitles copied the garbage\n// straight into the title. These assert the model no longer propagates it.\n//\n// Run: cd baml_src && baml-cli test\n\ntest regression_cjk_in_snippet {\n  functions [ImproveTitles]\n  args {\n    articles [\n      {\n        url \"https://x.com/TeksCreate/status/2075315553602814410\"\n        title \"GPT-5.6 agent workflows\"\n        source \"Aligned News\"\n        snippet #\"\n          GPT-5.6 focuses on supporting autonomous agents that operate for hours.\n          伊律(Meagle) this highlights durable workflows over raw benchmark score increases.\n          OpenAI had not previously publicized its agent capabilities as a key feature before now.\n        \"#\n      }\n    ]\n  }\n  @@assert(url_preserved, {{ this[0].url == \"https://x.com/TeksCreate/status/2075315553602814410\" }})\n  @@assert(no_markdown, {{ \"**\" not in this[0].title }})\n  @@assert(no_cjk, {{ \"伊\" not in this[0].title and \"律\" not in this[0].title }})\n  @@assert(single_line, {{ \"\\n\" not in this[0].title }})\n  @@assert(max_12_words, {{ this[0].title|split(\" \")|list|length <= 12 }})\n}\n\ntest regression_markdown_in_snippet {\n  functions [ImproveTitles]\n  args {\n    articles [\n      {\n        url \"https://www.therundown.ai/p/meta-sizes-up-gpt-5-5-with-watermelon\"\n        title \"Meta sizes up GPT-5.5 with Watermelon\"\n        source \"The Rundown AI\"\n        snippet #\"\n          Meta shares details on **Watermelon**, an internal GPT-5.5 benchmarking framework for ML experiments.\n          Frameworks like this test scalability, efficiency, and performance of large models.\n          Cursor Mobile also extends screenshot-to-bug-fix workflows for developers.\n        \"#\n      }\n    ]\n  }\n  @@assert(no_markdown, {{ \"**\" not in this[0].title and \"*\" not in this[0].title }})\n  @@assert(single_line, {{ \"\\n\" not in this[0].title }})\n  @@assert(no_json, {{ \"}\" not in this[0].title and \"]\" not in this[0].title }})\n  @@assert(max_12_words, {{ this[0].title|split(\" \")|list|length <= 12 }})\n}\n\n// Produced the truncated `**\"GPT-5.6` title in prod.\ntest regression_truncated_generation {\n  functions [ImproveTitles]\n  args {\n    articles [\n      {\n        url \"https://www.bensbites.com/p/grok-x-cursor\"\n        title \"Grok x Cursor\"\n        source \"Ben's Bites\"\n        snippet #\"\n          OpenAI updated ChatGPT with GPT-5.6 and a synthetic voice feature.\n          The changes include the ability to swap between voices in the free tier.\n          A new preview mode called 'Fable' introduces narrative-based interactions.\n        \"#\n      }\n    ]\n  }\n  @@assert(no_quote_wrapper, {{ '\"' not in this[0].title }})\n  @@assert(no_markdown, {{ \"**\" not in this[0].title }})\n  @@assert(complete_phrase, {{ this[0].title|split(\" \")|list|length >= 3 }})\n  @@assert(max_12_words, {{ this[0].title|split(\" \")|list|length <= 12 }})\n}\n\n// Produced the title with raw JSON (`}` / `]`) welded onto the end.\ntest regression_json_envelope_leak {\n  functions [ImproveTitles]\n  args {\n    articles [\n      {\n        url \"https://openai.com\"\n        title \"Build Week\"\n        source \"Aligned News\"\n        snippet #\"\n          SF developers can get live help and structured testing for frontier GPT-5.6 systems.\n          Local workshops guide them from concept to implementation.\n          The event offers hands-on access to AI system trials.\n        \"#\n      }\n    ]\n  }\n  @@assert(no_json, {{ \"}\" not in this[0].title and \"]\" not in this[0].title and \"{\" not in this[0].title }})\n  @@assert(single_line, {{ \"\\n\" not in this[0].title }})\n  @@assert(max_12_words, {{ this[0].title|split(\" \")|list|length <= 12 }})\n}\n\n// Thin snippet: the model must fall back to the original title, not invent one.\ntest regression_thin_snippet_keeps_original {\n  functions [ImproveTitles]\n  args {\n    articles [\n      {\n        url \"https://example.com/thin\"\n        title \"Anthropic ships Claude Code updates\"\n        source \"HN\"\n        snippet \"Read more.\"\n      }\n    ]\n  }\n  @@assert(kept_original, {{ this[0].title == \"Anthropic ships Claude Code updates\" }})\n}\n\n// Batch: URLs must not get crossed and content must not blend across articles.\ntest regression_batch_no_crosstalk {\n  functions [ImproveTitles]\n  args {\n    articles [\n      {\n        url \"https://example.com/a\"\n        title \"Meta releases Muse image model\"\n        source \"HN\"\n        snippet \"Meta made its Muse image model free on Instagram and WhatsApp, with API access.\"\n      }\n      {\n        url \"https://example.com/b\"\n        title \"vLLM news\"\n        source \"HN\"\n        snippet \"vLLM now integrates Hugging Face Transformers for production-grade inference serving.\"\n      }\n    ]\n  }\n  @@assert(both_returned, {{ this|list|length == 2 }})\n  @@assert(urls_distinct, {{ this[0].url != this[1].url }})\n  @@assert(no_blend, {{ \"Muse\" not in this[1].title }})\n}\n",
    "generators.baml": "generator python {\n  output_type \"python/pydantic\"\n  output_dir \"../\"\n  version \"0.223.0\"\n}\n",
    "score.baml": "class ScoredArticle {\n  url string\n  score int @description(\"1-100 developer-actionability rating\")\n  reason string @description(\"One short sentence naming what decided the score: out of scope, the evidence shown or missing, the reach, or the rule that capped it.\")\n}\n\n// Nemotron directly, no Nvidia fallback: falling back to another model would\n// mix two judges' scales in one feed (see the Nvidia wrapper in clients.baml). A NIM\n// outage holds the batch for the next run, as summaries already do.\nfunction ScoreArticles(articles: ArticleInput[]) -> ScoredArticle[] {\n  client Nemotron\n  prompt #\"\n    {{ _.role(\"system\") }}\n    You are a senior editorial analyst at a developer news platform. You rate articles on how much they help someone building with AI. You are precise and consistent, and when something is thin you say so with a low number.\n\n    {{ _.role(\"user\") }}\n    Score every article 1-100.\n\n    FIRST — IS IT IN SCOPE?\n    In scope: AI, ML, LLMs, foundation models, and the tools and infrastructure developers use to build with them.\n    Everything else scores 1-14 — stop there, do not read the rest of this guide: crypto/web3 (even when the post says \"agents\"), general programming (editors, parsers, version control, data structures, memory management), non-AI open source, security and formal verification with no AI component, classic ML/statistics tutorials (tree models, regression, tabular features), chip packaging and supply chain, regulation, politics, lawsuits, corporate drama, funding and hiring.\n\n    THEN — HOW MUCH DOES IT GIVE A BUILDER?\n    Two things decide the number:\n      EVIDENCE — what is actually shown? Runnable code, downloadable weights, a measured result with its method stated, or concrete steps a reader can copy. A number in a headline with no method behind it is a claim, not evidence — discount it hard, and the bigger the claim the harder.\n      REACH — how many people building with AI does it change something for? A frontier release or a runtime everyone uses is high. A weekend project, demo, or niche integration is low.\n\n    BANDS\n    Absolute, not relative to this batch. Every example below is a real item, correctly scored.\n\n    90-100 — Strong evidence, high reach. Frontier or open-weight releases you can run today, landmark results. Rare: a handful a month. 97+ is genuinely field-moving.\n      97  \"Kimi K3: Open Frontier Intelligence\" [Moonshot AI] — open 3T-class weights, frontier claims, downloadable.\n\n    80-89 — Real technical substance from a credible source, usable today. Also where a practitioner writeup lands when the technique is spelled out and reusable, first-party or not.\n      88  \"vLLM integrates Hugging Face Transformers for production-grade inference\" [HF Blog] — official integration, removes porting work for every model author.\n      85  \"TurboPrefill: 2.7x faster than llama.cpp Pipeline Parallel on Llama-3-70B\" [HN, 240 points] — upstream PR, stated hardware, measured speedup.\n      85  \"Optimizing Claude 3 Code's System Prompt for Efficiency and Output Quality\" [AI Hero] — third party, but the method is concrete and a reader can apply it.\n      85  \"Qwen3.6-35B-A3B\" [AI News] huggingface.co/Qwen/Qwen3.6-35B-A3B — found through an aggregator, but the URL is Qwen's own model card: a first-party open-weight release, not a rewrite.\n      82  \"Qwen3.6-27B KV Cache Quantization in vLLM: Accuracy, Memory, and Speed\" [Kaitchup] — measured comparison a reader can rerun.\n      80  \"Ollama ships native support for a major open model family\" [HN] — a runtime a huge share of builders already run, and the capability lands for all of them at once; adoption, not new research, so bottom of the band.\n\n    70-79 — Useful but narrow, or well-made with modest novelty. Community writeups, series posts, and think pieces that leave a builder with something belong here.\n      75  \"My self-sovereign local LLM setup, April 2026\" [personal blog] — no scripts, but named tooling and measured tok/sec across real hardware; a reader can rebuild from it.\n      74  \"Running Gemma 4 26B at 5 tokens/sec on a 13-year-old Xeon with no GPU\" [HN] — reproducible with exact flags, but narrow.\n      74  \"Orchestrating AI code review at scale\" [Hacker News] blog.cloudflare.com — seven review agents per merge request, architecture and trade-offs spelled out; no A/B numbers, but a team can copy the design.\n      72  \"Running a one-trillion-parameter LLM locally on an AMD Ryzen AI Max+ cluster\" [Hacker News] amd.com — exact hardware and llama.cpp RPC steps; reproducible without a benchmark table.\n      72  \"We measured agent token cost across 40 repos before picking a context strategy\" [Eng blog] — written for founders and leads, not the coder, but real data with a stated method and a decision it drove.\n      70  \"What 18 agent-built PRs cost us in context tokens\" [Vendor blog] — measured from their own sessions with the method stated and it names a real cost line item; it also pitches their product, which neither adds nor subtracts.\n      70  \"Set Up a $0 Cloud VM to Run Your Hermes Agent (Part 1)\" [AI Weekender] — serial newsletter post, but the steps work.\n      70  \"Why Human Language is the New Code (and How to Write It)\" [Substack] — think piece, worth a builder's time.\n      70  \"Orchestrating agents just got 10x easier, check out Temporal\" [x.com] — social post, but a real tool behind it.\n\n    55-69 — Minor tools, incremental product news, competent commentary, demos.\n      65  \"A month of local-LLM releases, rounded up\" [Substack] — competent digest, saves some tab-hopping, but no analysis of its own.\n      62  \"llama.cpp adds a --cache-reuse flag for partial prompt matches\" [HN] — real and merged upstream, but a single knob most builders never touch.\n      60  \"How Heurist Finance built an AI-native investment workbench on Amazon Bedrock AgentCore\" [AWS Machine Learning] — a customer story selling the platform; architecture boxes, no measured result.\n      60  \"A Mathematical Framework for Transformer Circuits\" [HN, transformer-circuits.pub/2021/...] — foundational, but a 2021 paper resurfacing years later is not news.\n      55  \"I built a TUI for chatting with Ollama\" [HN] — it works, but it is small and a dozen others do the same thing.\n      55  \"GLM-5.3 is 50% off through DigitalOcean on AI Gateway\" [Vercel] — a price notice; the model release it points at was the news.\n\n    35-54 — Unverified solo projects, marketing and landing pages, big numbers with no method.\n      45  \"LLMrPro: free LLM balancer combines local machines with cloud fallback\" [HN, 4 points, 0 comments] — unknown solo repo in a crowded category, and nobody read it.\n      45  \"Pinecone vs Amazon S3 Vectors\" [Pinecone] — the vendor's own comparison page: a sales page, not an article.\n      35  \"SigMap cuts prompt count by 49% across 90 coding tasks\" [HN, 3 points, 1 comment] — landing page, extraordinary numbers, no method or repo.\n\n    15-34 — Slop, SEO bait, advocacy and campaign pages, manifestos, content-free.\n\n    1-14 — Out of scope only. An in-scope article never goes below 15: this band means \"wrong subject\", not \"bad\".\n\n    RULES\n    - A first-party release post on a major lab's own domain (openai.com, anthropic.com, deepmind.google, mistral.ai, kimi.com, ai.meta.com, qwen.ai, huggingface.co/blog, an official model card) never scores below 70. Aggregators retitle these into something that reads like a rewrite — judge the release, not the headline someone put on it. The floor is for releases — a model, API, tool or capability shipping — not for everything on the domain: a customer story, case study, event recap or marketing page on a lab's site is judged like any vendor post.\n    - The [source] tag says where we found the link — often an aggregator, a newsletter or Hacker News — not who wrote it. Decide first-party versus rewrite from the URL: the maker's own site, blog, docs, x.com account, huggingface.co/<maker> or github.com/<maker> is first-party, whatever the tag says. A company's own release of its own model or developer tool there is a release and is judged as one — never as a notice or a rewrite.\n    - A new model that builders can download or call today, released by an established maker — a known lab, a major tech company, or the company behind a tool a large share of builders already use (Cursor, Black Forest Labs, Xiaomi, Microsoft, NVIDIA, DeepSeek) — is a release even when the maker is not on the list above. It lands 70 or above, with REACH deciding how far; its self-reported benchmarks neither add nor subtract. An unknown company announcing a model on a landing page with big claims and no weights, API docs or model card is still a landing page (35-54).\n    - A piece written for founders or tech leads — someone deciding what their team builds with, not writing the code — is in scope, judged on the same EVIDENCE bar as everything else: data with a stated method, a named decision it informs, or steps someone can follow. One that clears it lands 55-79. A polished piece with only an argument behind it still caps at 40, as do pure careers, business strategy, funding, and hiring. \"The model is the brain, everything else is the desk\" is a metaphor, not evidence.\n    - A vendor's own blog post is scored on its evidence like anything else. Real numbers with a stated method count even when the post also pitches the vendor's product; a product pitch with no method behind it does not (see the 35-54 band).\n    - A customer case study on a cloud or platform vendor's blog (\"How X built Y with Bedrock / AgentCore / Vertex / Azure\") is marketing for the platform: it caps at 60 unless it reports a measured result with its method stated.\n    - An availability or pricing notice — an existing model or tool now available on another platform, a price cut, a discount, a new region or quota — caps at 55. The release it points at is the news; this is a changelog line. A new model or product launching is a release, not a notice, however short the post that announces it.\n    - A third-party rewrite of someone else's release or paper (news aggregators, SEO explainers, reseller and wrapper sites) caps at 80: the evidence lives in the primary source, not the retelling. One that only restates the announcement, adding nothing, caps at 60.\n    - A page that is not an article — a documentation index, a product or landing page, a model catalog, a pricing page, a vendor's own \"X vs Y\" comparison — belongs in 35-54, however strong the product behind it. So does a standing page for something that already exists — a product homepage, a model or tool page with no launch news on it — even on a lab's own domain: the release floor is for launches.\n    - Old work resurfacing is not news. If the URL or the text dates the piece more than six months before the seen date (a /2021/ path, a 2024 arXiv id, \"published March 2025\"), it caps at 60, however good it is.\n    - An independent or personal developer blog is scored on the same EVIDENCE and REACH as anything else — no penalty for having no institution behind it, no bonus for the author's name. A setup writeup, a debugging story, or a benchmark on the author's own hardware lands where its detail puts it: concrete and reproducible reaches 70-79, a spelled-out reusable technique 80-89, thin or purely reflective posts below.\n    - Missing numbers are not missing evidence. Exact setup steps, or a system design spelled out in enough detail for a team to copy it, are evidence on their own: such a piece lands 70-79 even with no benchmark table. \"No measured results\" only drags down a piece with nothing else behind it — a claim, a pitch, a landing page. Working code alone does not lift a small tool: a weekend repo that runs is still a minor tool (55-69).\n    - A proposed spec, standard, or protocol with no users beyond its author caps at 70, even with a reference implementation. Anyone can publish a spec; adoption is what makes one matter.\n    - Empty snippet means you are judging a title alone → cap 70.\n    - Score against the whole field, not against the others in this batch. A weak batch produces low scores; that is the correct outcome.\n    - Topic is never a bonus. Local inference, quantization, privacy, on-device, and token cost are common subjects, not merit. A shallow post about local inference scores below a rigorous post about anything else.\n    - Trust tag: high +3, low -3, applied after the band is chosen. It breaks ties; it does not move an article between bands.\n    - Traction, when given, is how the public actually received the item where it was posted, and it is evidence about REACH — the one thing a title cannot fake. A submission sitting at single-digit points and no comments is a post nobody read: it caps at 45 regardless of how the title is written, and \"Show HN\"-style self-promotion there caps at 35. Strong traction (hundreds of points, a live discussion) confirms reach; it does not add evidence, so it never lifts a thin item past 70 on its own. No traction field means the source has no such signal — judge on evidence alone, do not assume either way.\n    - Use the whole range. 74, 68, and 52 are real scores. Most items are not 90s: if you are about to give a batch of five four scores above 85, you have stopped discriminating. Re-read the bands and place each one absolutely.\n\n    Articles:\n    {% for a in articles %}\n    {{ loop.index }}. [{{ a.source }}] \"{{ a.title }}\" - {{ a.url }}{% if a.trust %} [trust: {{ a.trust }}]{% endif %}{% if a.traction %} [traction: {{ a.traction }}]{% endif %}{% if a.seen_on %} [seen: {{ a.seen_on }}]{% endif %}\n    {% if a.snippet %}   {{ a.snippet }}{% else %}   (no content available){% endif %}\n    {% endfor %}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\ntest score_frontier_release {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://www.kimi.com/blog/kimi-k3\"\n        title \"Kimi K3: Open Frontier Intelligence\"\n        source \"Moonshot AI\"\n        snippet \"Kimi K3 is the world's first open 3T-class model - frontier performance across coding, knowledge work, and reasoning.\"\n        trust \"medium\"\n      }\n    ]\n  }\n}\n\ntest score_out_of_scope {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://x.com/NEARProtocol/status/2076729054632231274\"\n        title \"NEAR Launches General Availability of Confidential Intents via 1Click Swap API for Developer Integration\"\n        source \"Aligned News\"\n        snippet \"NEAR released Confidential Intents for general use through its 1Click Swap API.\"\n        trust \"medium\"\n      }\n    ]\n  }\n}\n\n// Separates practitioner content from advocacy. The newsletter walkthrough has\n// steps a reader can follow and should clear 65; the campaign page has nothing\n// technical and stays below 35.\ntest score_practitioner_vs_advocacy {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://aiweekender.substack.com/p/set-up-a-0-cloud-vm-to-run-your-hermes\"\n        title \"Set Up a $0 Cloud VM to Run Your Hermes Agent (Part 1)\"\n        source \"AI Weekender\"\n        snippet \"This post is the first in a series. It details setting up a free, always-on Linux virtual machine. This VM will host a Hermes agent.\"\n        trust \"medium\"\n      }\n      {\n        url \"https://righttointelligence.org/\"\n        title \"Protect your right to run local AI\"\n        source \"Hacker News\"\n        snippet \"A campaign page advocating the right to run local AI. It calls for protecting the ability to run models on your own hardware.\"\n        trust \"medium\"\n        traction \"18 points, 6 comments on Hacker News\"\n      }\n    ]\n  }\n}\n\n// Founder-audience is in scope. The piece with measured data and a decision\n// behind it clears the threshold; the polished argument with nothing to act on\n// stays at or under 40; a vendor post with real numbers and a stated method\n// clears it too, its product pitch notwithstanding.\ntest score_founder_audience {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://example.com/blog/agent-token-cost-study\"\n        title \"We measured agent token cost across 40 repos before picking a context strategy\"\n        source \"Engineering blog\"\n        snippet \"Over six weeks we logged context-token usage for coding agents across 40 internal repos, with the measurement method and per-repo numbers. Here is how it shaped the retrieval approach we chose.\"\n        trust \"medium\"\n      }\n      {\n        url \"https://example.com/blog/why-agents-change-the-org\"\n        title \"Why AI agents change how you should structure your engineering org\"\n        source \"Substack\"\n        snippet \"A case that agents shift the unit of work from the team to the individual, and what that means for how founders should plan headcount.\"\n        trust \"medium\"\n      }\n      {\n        url \"https://vendor.example.com/blog/stop-the-context-tax\"\n        title \"What 18 agent-built PRs cost us in context tokens\"\n        source \"Vendor blog\"\n        snippet \"Each agent-built PR in our codebase averaged $65 in context-token overhead, peaking near 975k tokens. Our semantic navigation layer cuts that.\"\n        trust \"medium\"\n      }\n    ]\n  }\n}\n\n// Regression: a big percentage in a title with nothing behind it must not beat\n// a measured result. Expect the landing page well below the benchmark post.\ntest score_unverified_claim_vs_measured {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://sigmap.io/\"\n        title \"SigMap cuts prompt count by 49% across 90 coding tasks\"\n        source \"Hacker News\"\n        snippet \"SigMap cuts prompt count by 49.2% on 90 coding tasks. It reduces token usage by 97% and runs offline with zero dependencies.\"\n        trust \"medium\"\n        traction \"3 points, 1 comments on Hacker News\"\n      }\n      {\n        url \"https://github.com/ggml-org/llama.cpp/pull/24219\"\n        title \"TurboPrefill: 2.7x faster than llama.cpp Pipeline Parallel on Llama-3-70B\"\n        source \"Hacker News\"\n        snippet \"TurboPrefill implements intra-prompt pipeline scheduling for multi-GPU prefill. On Llama-3-70B it runs 2.7x faster than llama.cpp pipeline parallel.\"\n        trust \"medium\"\n        traction \"240 points, 96 comments on Hacker News\"\n      }\n    ]\n  }\n}\n\n// The 2026-09-13 caps, all from a review of the ten runs before it. Expect the\n// discount notice at or under 55, the AWS customer story at or under 60, the\n// comparison page in 35-54, and the resurfaced 2021 paper at or under 60.\ntest score_notices_case_studies_and_old_work {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://vercel.com/changelog/glm-5-3-is-50-off-through-digitalocean-on-ai-gateway\"\n        title \"GLM-5.3 is 50% off through DigitalOcean on AI Gateway\"\n        source \"Vercel\"\n        snippet \"GLM-5.3 is now 50% off when routed through DigitalOcean on AI Gateway, through the end of the month.\"\n        trust \"medium\"\n        seen_on \"2026-09-03\"\n      }\n      {\n        url \"https://aws.amazon.com/blogs/machine-learning/how-heurist-finance-built-an-ai-native-investment-workbench-on-amazon-bedrock-agentcore/\"\n        title \"How Heurist Finance built an AI-native investment workbench on Amazon Bedrock AgentCore\"\n        source \"AWS Machine Learning\"\n        snippet \"Heurist Finance uses Amazon Bedrock AgentCore to turn a chat into full-stack financial research. AgentCore payments buy the premium data each query needs.\"\n        trust \"medium\"\n        seen_on \"2026-09-10\"\n      }\n      {\n        url \"https://www.pinecone.io/compare/elasticsearch\"\n        title \"Pinecone vs Elasticsearch\"\n        source \"Pinecone\"\n        snippet \"Pinecone separates storage and compute, so there is no cluster to manage. Elasticsearch stores vectors in the same shards as text.\"\n        trust \"medium\"\n        seen_on \"2026-09-05\"\n      }\n      {\n        url \"https://transformer-circuits.pub/2021/framework/index.html\"\n        title \"A Mathematical Framework for Transformer Circuits\"\n        source \"Hacker News\"\n        snippet \"We propose a way to reverse engineer transformer computations, mapping layers to readable steps.\"\n        trust \"medium\"\n        traction \"132 points, 21 comments on Hacker News\"\n        seen_on \"2026-09-13\"\n      }\n    ]\n  }\n}\n\n// From the 2026-09-13 rescore, where Nemotron read the aggregator in [source]\n// as the author and called first-party releases rewrites or notices, and\n// dropped a copyable engineering write-up for having no benchmark table.\n// Expect all three at 70 or above.\ntest score_source_tag_is_not_the_author {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://huggingface.co/Qwen/Qwen3.6-35B-A3B\"\n        title \"Qwen3.6-35B-A3B sparse MoE model beats Qwen3.5-27B and Gemma4-31B\"\n        source \"AI News\"\n        snippet \"A new sparse Mixture-of-Experts LLM with 35B total parameters that runs with only 3B active delivers better coding and vision performance than larger dense models.\"\n        trust \"medium\"\n        seen_on \"2026-04-17\"\n      }\n      {\n        url \"https://x.com/MicrosoftAI/status/2061887500541366489\"\n        title \"Microsoft AI unveils seven new MAI models across reasoning, code, image, speech\"\n        source \"AI News\"\n        snippet \"Microsoft AI releases seven new models, including a data-lineage-first reasoning model. The MAI models cover reasoning, code, image, text-to-speech and transcription.\"\n        trust \"medium\"\n        seen_on \"2026-06-03\"\n      }\n      {\n        url \"https://blog.cloudflare.com/ai-code-review/\"\n        title \"Orchestrating AI code review at scale\"\n        source \"Hacker News\"\n        snippet \"Cloudflare built a CI system that runs up to seven AI code-review agents per merge request and posts a single structured comment, with the architecture and trade-offs laid out.\"\n        trust \"medium\"\n        seen_on \"2026-05-30\"\n      }\n    ]\n  }\n}\n\n// A usable model from an established maker is a release even off the lab list;\n// an unknown company's model landing page with extraordinary claims is not.\n// Expect Cursor's Composer at 70 or above and SubQ in 35-54.\ntest score_established_release_vs_unknown_landing_page {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://cursor.com/blog/2-0\"\n        title \"Cursor 2.0 adds Composer model with 4x faster coding\"\n        source \"Ben's Bites\"\n        snippet \"Cursor 2.0 releases Composer, a new coding model that runs four times faster, available in the editor today.\"\n        trust \"medium\"\n        seen_on \"2026-03-15\"\n      }\n      {\n        url \"https://subq.ai/\"\n        title \"SubQ: Sub-quadratic LLM for 12-million-token reasoning\"\n        source \"Hacker News\"\n        snippet \"SubQ lets you reason over 12 million tokens in one prompt, at one-fifth the cost of other LLMs. 150 tokens/s, cuts attention compute ~1,000x.\"\n        trust \"medium\"\n        seen_on \"2026-05-07\"\n      }\n    ]\n  }\n}\n\n// Regression for the failure this rubric's traction rule exists to fix: an\n// unread Show HN repo and a lab release, judged on the same title-shaped\n// evidence. Before traction was passed through, batches like this came back\n// with both in the 90s. Expect the repo in the 35-54 band and the release well\n// clear of it.\ntest score_unread_repo_vs_lab_release {\n  functions [ScoreArticles]\n  args {\n    articles [\n      {\n        url \"https://github.com/Gysho/LLMrPro\"\n        title \"Free LLM balancer combines multiple local inference machines with cloud fallback\"\n        source \"Hacker News\"\n        snippet \"LLMrPro balances requests across local inference machines and falls back to the cloud.\"\n        trust \"medium\"\n        traction \"4 points, 0 comments on Hacker News\"\n      }\n      {\n        url \"https://www.anthropic.com/news/claude-sonnet-5\"\n        title \"Claude Sonnet 5: More agentic model with planning and tool use\"\n        source \"Hacker News\"\n        snippet \"Claude Sonnet 5 is available today across the API, with stronger planning and tool use.\"\n        trust \"medium\"\n        traction \"612 points, 341 comments on Hacker News\"\n      }\n    ]\n  }\n}\n",
    "shared.baml": "// Shared types used by multiple BAML functions.\n\nenum ArticleCategory {\n  Models\n  Dev\n  Research\n}\n\nenum ArticleKind {\n  Repo         // GitHub/GitLab repository\n  Paper        // Academic paper, preprint, technical report\n  Model        // Model card or model release page\n  Blog         // Developer blog post, tutorial, how-to, opinion piece\n  Product      // Product launch page or product website\n  Announcement // Official release post from a major AI lab\n}\n\n// Used as input to ScoreArticles, ImproveTitles.\n// Mirrors the fields the existing formatArticles() helper emits.\nclass ArticleInput {\n  url string\n  title string\n  source string\n  snippet string? @description(\"First ~200 chars of the article content, if any\")\n  trust string? @description(\"\\\"high\\\" | \\\"medium\\\" | \\\"low\\\" - source trust tag\")\n  traction string? @description(\"How the item was received where it was posted, e.g. \\\"7 points, 1 comments on Hacker News\\\". Absent for sources with no public reception signal.\")\n  seen_on string? @description(\"Date the pipeline picked the item up, YYYY-MM-DD. What 'old' is measured against: a feed or HN date is when it was posted there, not when the work was published.\")\n}\n",
    "summarize.baml": "// The summary shown under an article's title. Called by\n// pipeline/steps/summarize.py before insert (an article it cannot summarize is\n// not inserted), and by scripts/summarize_articles.py to backfill older rows.\n//\n// Nemotron directly, no Nvidia fallback: a summary that silently came from\n// Ministral would be written in a different voice. A NIM outage holds the\n// run's articles for the next run instead.\n//\n// Its own client, not the shared `Nemotron` in clients.baml: that one carried\n// `timeout 120` in options, which integrate.api.nvidia.com rejects as an\n// unsupported body param (400). It sets its timeout in an `http` block now,\n// so the two could be merged.\nclient<llm> NemotronSummary {\n  provider openai-generic\n  retry_policy Burst\n  options {\n    base_url \"https://integrate.api.nvidia.com/v1\"\n    api_key env.NVIDIA_NIM_API_KEY\n    model \"nvidia/nemotron-3-super-120b-a12b\"\n  }\n}\n\nfunction SummarizeArticle(title: string, content: string, bullet_count: int) -> string {\n  client NemotronSummary\n  prompt #\"\n    {{ _.role(\"system\") }}\n    You write short article summaries in the voice of Julia Evans (jvns.ca):\n    warm, curious, plain-spoken, concrete.\n\n    Voice rules:\n    - Short sentences. Everyday words. Contractions are fine.\n    - Explain any jargon in a few words the first time it appears.\n    - Enthusiastic but grounded - \"this is a neat idea\" is fine, \"game-changing\" is not.\n    - Say what a reader actually learns or can do.\n    - No marketing language, no hype, no rhetorical build-up, no closing pep talk.\n    - Do NOT invent first-person experience (\"I tried this and...\") or any fact\n      the article does not state. Summarize only what is there.\n    - Write in English only.\n\n    {{ _.role(\"user\") }}\n    Summarize the article below as a hook line followed by bullets.\n\n    Format, exactly:\n    - Line 1: the hook - one plain sentence saying what this is and why a\n      developer would care. No label, no bullet character.\n    - Then {{ bullet_count }} lines, each starting with \"- \", one concrete\n      detail each: how it works, numbers, requirements, what's new. Terse,\n      not full sentences where a fragment does.\n    - If the article states a real limitation or open question, add one final\n      line starting with \"- Catch: \".\n\n    No blank lines. No headers. No numbering. No markdown bold or backticks.\n\n    Title: {{ title }}\n\n    Content:\n    {{ content }}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// ─── Tests ──────────────────────────────────────────────────────────────────\n// Format asserts mirror the prompt: a hook line, then exactly `bullet_count`\n// bullets, then an optional \"- Catch:\" line, no blank lines.\n//\n// Line checks use `lines` and `regex_match`, never a \"\\n\" string: inside an\n// assert \"\\n\" stays a literal backslash-n, so `\"\\n\" in this` is always false\n// and `split(\"\\n\")` never splits. regex_match reads the same \\n as a newline.\n//\n// The CJK cases matter most. pipeline/steps/summarize.py rejects a summary\n// with a single CJK (or Cyrillic, Arabic, Hebrew) character anywhere, and a\n// rejected summary means the article is not inserted.\n//\n// Run: cd backend && uv run --env-file ../.env baml-cli test --from baml_src -i \"SummarizeArticle::\"\n\ntest summarize_stated_limitation_gets_a_catch {\n  functions [SummarizeArticle]\n  args {\n    bullet_count 3\n    title \"A tiny CLI for tailing Postgres logs\"\n    content #\"\n      pgtail is a small command-line tool that follows a Postgres server's log\n      file and colourises slow queries. It reads the `log_min_duration_statement`\n      setting to decide what counts as slow. It has no dependencies beyond libpq\n      and is a single Go binary. The author notes it does not yet handle log\n      rotation, so a rotated file stops being followed until you restart it.\n    \"#\n  }\n  @@assert(hook_first, {{ this[:2] != \"- \" }})\n  @@assert(no_blank_lines, {{ not (this|trim|regex_match(\"(?s).*\\n\\s*\\n.*\")) }})\n  @@assert(hook_bullets_catch, {{ this|trim|lines|list|length == 5 }})\n  @@assert(has_catch, {{ \"- Catch:\" in this }})\n}\n\ntest summarize_no_limitation_no_catch {\n  functions [SummarizeArticle]\n  args {\n    bullet_count 4\n    title \"uv 0.9 adds an outdated-dependency view\"\n    content #\"\n      uv 0.9 ships `uv tree --outdated`, which prints the dependency tree with\n      the latest available version next to each package that is behind. It\n      reads the lockfile, so it needs no network call for the tree itself, only\n      for the version lookup. The release also makes `uv sync` about 20% faster\n      on large workspaces by caching resolved markers between runs, and adds\n      `uv python pin --global` to set a default interpreter for every project.\n      Windows users get native ARM64 builds. Upgrading is `uv self update`.\n    \"#\n  }\n  @@assert(hook_first, {{ this[:2] != \"- \" }})\n  @@assert(no_blank_lines, {{ not (this|trim|regex_match(\"(?s).*\\n\\s*\\n.*\")) }})\n  @@assert(hook_and_4_bullets, {{ this|trim|lines|list|length == 5 }})\n  @@assert(no_invented_catch, {{ \"Catch:\" not in this }})\n}\n\ntest summarize_six_bullets_for_a_long_article {\n  functions [SummarizeArticle]\n  args {\n    bullet_count 6\n    title \"Serving a 70B model on two consumer GPUs\"\n    content #\"\n      This post walks through serving a 70-billion-parameter model on two RTX\n      4090 cards (24GB each) for a small internal team. The model is quantized\n      to 4-bit with AWQ, which brings the weights to about 38GB, so they are\n      split across both cards with tensor parallelism in vLLM.\n\n      Throughput settles at 28 tokens per second for a single user and 190\n      tokens per second aggregate at 16 concurrent requests. The KV cache is the\n      real limit: with 4-bit weights there is room for roughly 12K tokens of\n      context per request at that concurrency. The author sets\n      `--max-model-len 16384` and lets vLLM's paged attention handle the rest.\n\n      Power draw peaks at 820W for the whole box, so the author capped each card\n      at 300W with `nvidia-smi -pl 300`, which cost about 8% throughput and\n      dropped temperatures by 12 degrees.\n\n      Quality was checked on the team's own 200-question eval: the 4-bit model\n      scored 81% against 84% for the fp16 version run on a rented H100. The\n      author considers that close enough for code review and drafting.\n\n      The full setup, including the systemd unit and the Caddy reverse proxy\n      config, is in the linked repo. Total hardware cost was about $3,600.\n      The one open problem is cold start: loading the weights takes 4 minutes,\n      so the service is never scaled to zero.\n    \"#\n  }\n  @@assert(hook_first, {{ this[:2] != \"- \" }})\n  @@assert(no_blank_lines, {{ not (this|trim|regex_match(\"(?s).*\\n\\s*\\n.*\")) }})\n  @@assert(hook_6_bullets_maybe_catch, {{ this|trim|lines|list|length in [7, 8] }})\n}\n\ntest summarize_chinese_article_in_english {\n  functions [SummarizeArticle]\n  args {\n    bullet_count 3\n    title \"Qwen3-Coder-Mini 发布\"\n    content #\"\n      Qwen 团队发布了 Qwen3-Coder-Mini，一个 70 亿参数的开源代码模型。它支持\n      128K 上下文，在单张 24GB 显卡上即可运行。模型采用 Apache 2.0 许可证，\n      权重已上传到 Hugging Face。团队表示，它在 HumanEval 上得分 82%，但在长文件\n      重构任务上仍落后于更大的模型。\n    \"#\n  }\n  @@assert(no_cjk, {{ \"团队\" not in this and \"模型\" not in this and \"开源\" not in this and \"上下文\" not in this and \"显卡\" not in this and \"发布\" not in this }})\n  @@assert(hook_first, {{ this[:2] != \"- \" }})\n  @@assert(no_blank_lines, {{ not (this|trim|regex_match(\"(?s).*\\n\\s*\\n.*\")) }})\n}\n\ntest summarize_does_not_copy_a_native_name {\n  functions [SummarizeArticle]\n  args {\n    bullet_count 3\n    title \"Alibaba ships a Qwen3.8 preview\"\n    content #\"\n      Alibaba's Tongyi Qianwen team (通义千问) released a preview of Qwen3.8,\n      an open-weight model with a 1M-token context window. The team says the\n      preview is updated daily during the preview period; its announcement post\n      was titled \"持续变强\" (\"getting stronger every day\"). Weights are on\n      Hugging Face under the Qwen license. Reported gains are largest on web\n      frontend generation, where it scores 71% on the team's own WebDev bench,\n      up from 58% for Qwen3.5. No third-party evaluations exist yet.\n    \"#\n  }\n  @@assert(no_cjk, {{ \"通义千问\" not in this and \"持续变强\" not in this and \"通义\" not in this }})\n  @@assert(hook_first, {{ this[:2] != \"- \" }})\n  @@assert(no_blank_lines, {{ not (this|trim|regex_match(\"(?s).*\\n\\s*\\n.*\")) }})\n}\n\ntest summarize_strips_markdown_from_the_source {\n  functions [SummarizeArticle]\n  args {\n    bullet_count 3\n    title \"Why we moved our job queue off Redis\"\n    content #\"\n      ## Why we moved off Redis\n\n      **TL;DR:** we replaced our Redis job queue with Postgres and `SKIP LOCKED`.\n\n      ### The problem\n\n      Redis was a second stateful service to back up and monitor, and a\n      failover once lost about 400 queued jobs. Our peak load is **2,000 jobs\n      per minute**, well within what Postgres handles.\n\n      ### The fix\n\n      ```sql\n      SELECT id FROM jobs WHERE state = 'queued'\n      ORDER BY created_at FOR UPDATE SKIP LOCKED LIMIT 10;\n      ```\n\n      Workers poll every 500ms. Jobs now commit in the same transaction as the\n      data they touch, so a job can never run against a row that was rolled\n      back. p99 pickup latency went from 40ms to 180ms, which we accepted.\n    \"#\n  }\n  @@assert(no_bold, {{ \"**\" not in this }})\n  @@assert(no_backticks, {{ \"`\" not in this }})\n  @@assert(no_headers, {{ \"##\" not in this }})\n  @@assert(hook_first, {{ this[:2] != \"- \" }})\n  @@assert(no_blank_lines, {{ not (this|trim|regex_match(\"(?s).*\\n\\s*\\n.*\")) }})\n}\n\ntest summarize_drops_the_hype {\n  functions [SummarizeArticle]\n  args {\n    bullet_count 3\n    title \"Introducing FlowForge AI\"\n    content #\"\n      Introducing FlowForge AI - the revolutionary, game-changing platform that\n      will transform how your team builds agents forever! Unleash the power of\n      AI today.\n\n      FlowForge lets you chain LLM calls in a visual editor and export the\n      result as a plain Python script. It supports OpenAI and Anthropic models\n      out of the box, with retries and a per-step cost readout. The free tier\n      includes 1,000 runs per month; the paid plan is $29 per month per seat.\n      Self-hosting is not available yet.\n    \"#\n  }\n  @@assert(no_hype, {{ \"revolutionary\" not in this|lower and \"game-changing\" not in this|lower and \"unleash\" not in this|lower }})\n  @@assert(hook_first, {{ this[:2] != \"- \" }})\n  @@assert(no_blank_lines, {{ not (this|trim|regex_match(\"(?s).*\\n\\s*\\n.*\")) }})\n}\n",
}

def get_baml_files():
    return _file_map